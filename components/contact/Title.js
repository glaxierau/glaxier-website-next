import React from 'react'

const ContactTitle = ({ title }) => {
    return (
        <div>
            <h3 className="text-2xl font-black text-center py-10">{title}</h3>
        </div>
    )
}

export default ContactTitle
